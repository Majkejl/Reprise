name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: Reprise/package-lock.json

      - name: Install dependencies
        working-directory: Reprise
        run: npm ci

      - name: Lint
        working-directory: Reprise
        run: npm run lint

      - name: Build
        working-directory: Reprise
        env:
          VITE_BASE_PATH: /reprise/
        run: npm run build
