// LessonReader.tsx — read-only view of a lesson's overview and all cards as Q&A pairs.
// DEFERRED (Phase 3): display lesson overview and cards without starting a session.

export function LessonReader() {
  return (
    <div className="px-4 py-8 max-w-lg mx-auto">
      <p className="text-zinc-500 text-sm">Lesson Reader — coming in Phase 3</p>
    </div>
  )
}
