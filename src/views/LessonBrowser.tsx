// LessonBrowser.tsx — browse and filter all locally cached lessons by tag and source.
// DEFERRED (Phase 3): list all lessons in local DB, filterable by tag and source.

export function LessonBrowser() {
  return (
    <div className="px-4 py-8 max-w-lg mx-auto">
      <p className="text-zinc-500 text-sm">Lesson Browser — coming in Phase 3</p>
    </div>
  )
}
