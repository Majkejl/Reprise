// ScopePicker.tsx — select which sources and tags are in scope for study sessions.
// DEFERRED (Phase 3): scope selection UI; persists to Settings via UI & Scope Store.

export function ScopePicker() {
  return (
    <div className="px-4 py-8 max-w-lg mx-auto">
      <p className="text-zinc-500 text-sm">Scope Picker — coming in Phase 3</p>
    </div>
  )
}
